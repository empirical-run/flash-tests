import { test } from "@playwright/test";

test("test group preview caching", async ({ page }) => {
  //
  // login to flash with arush@empirical.run and password QncNV6!u!FTcTBF
  // let the dashboard open
  // click on the add button
  // type the group preview name "preview"
  // test the the group preview is upto date
  //
});
