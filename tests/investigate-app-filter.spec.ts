import { test, expect } from "./fixtures";

test('investigate App issue type filter behavior', async ({ page }) => {
  await page.goto('/');
  // TODO(agent on page): Navigate to Issues page, apply filter for App issue type, and check what results are actually shown in the table
});